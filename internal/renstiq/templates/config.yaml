# リポジトリ探索専用の設定。処理ルールは各repoの renstiq.yaml に記述する。
# renstiq discover --config FILE で別の探索設定を指定できる。
version: 2

discovery:
  # 探索対象のディレクトリ。絶対パス（~/ も可）の glob。
  # * は1階層、** は再帰的に探索する。例: ["/path/to/repositories/*/"]
  include: []
  # 探索から除外するパス。例: ["/path/to/repositories/archived-*/"]
  exclude: []
